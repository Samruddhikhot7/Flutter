import 'package:flutter/material.dart'; // 1️⃣ Importing Flutter material package

void main() {                          // 2️⃣ main() function - entry point
  runApp(MyApp());                     // 3️⃣ runApp() - runs the root widget
}

class MyApp extends StatelessWidget {  // 4️⃣ MyApp is a StatelessWidget
  @override
  Widget build(BuildContext context) { // 5️⃣ build() method returns the widget tree
    return MaterialApp(                // 6️⃣ MaterialApp - sets up the app environment
      home: HomeScreen(),              // 7️⃣ home - sets the default screen
    );
  }
}

class HomeScreen extends StatelessWidget { // 8️⃣ Another StatelessWidget
  @override
  Widget build(BuildContext context) {
    return Scaffold(                     // 9️⃣ Scaffold - basic visual layout structure
      appBar: AppBar(                    // 🔟 AppBar - top bar
        title: Text('Stateless Widget'), // 11️⃣ Text widget inside AppBar
      ),
      body: Center(                      // 12️⃣ Center - centers its child
        child: Container(
          child :Text(                     // 13️⃣ Text - displays static text
          'Hello, Flutter!',style: TextStyle(fontSize: 24),),
          color: Colors.blue,// 14️⃣ Styling the text
        ),
      ),
    );
  }
}
